import React from "react";
import { PacmanLoader } from "react-spinners";
const Spinner = () => {
    return (
        <div>
            <PacmanLoader color="black" size={100} />
        </div>
    );
};

export default Spinner;
